[pytest]
testpaths = tests
markers =
    unit: fast, self-contained tests with no external services (CI default)
    integration: require live services (Kafka, Cassandra, observability stack) or internet access
